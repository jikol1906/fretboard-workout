import * as React from 'react';
import { Grid } from 'theme-ui';

interface INoteButtonContainerProps {
}

const NoteButtonContainer: React.FunctionComponent<INoteButtonContainerProps> = (props) => {
  return <Grid>
      
  </Grid>;
};

export default NoteButtonContainer;
